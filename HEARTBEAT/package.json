{
  "name": "tg-heartbeat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vercel cron function to ping Render API /health and prevent sleep",
  "scripts": {
    "dev": "vercel dev",
    "start": "node --version"
  },
  "engines": {
    "node": ">=18"
  }
}
